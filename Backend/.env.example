# Configuración del Backend de WiiCare
# Copia este archivo como .env y ajusta los valores según tu entorno

# Entorno de ejecución
NODE_ENV=development

# Puerto del servidor
PORT=4000

# Base de datos MongoDB
# Para desarrollo local:
MONGODB_URI=mongodb://localhost:27017/wiicare

# Para MongoDB Atlas:
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/wiicare?retryWrites=true&w=majority

# JWT Secret (IMPORTANTE: Cambia esto en producción por una clave segura)
JWT_SECRET=your_secret_key_here

# JWT Expiration
JWT_EXPIRES_IN=7d

# CORS Origins (separados por coma)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:4200

# Opciones adicionales
LOG_LEVEL=info

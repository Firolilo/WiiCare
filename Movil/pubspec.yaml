name: wiicare_movil
description: WiiCare - Plataforma móvil para cuidadores y personas con necesidades especiales
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # UI & Navigation
  cupertino_icons: ^1.0.6
  go_router: ^13.0.0
  
  # State Management
  provider: ^6.1.1
  
  # HTTP & API
  http: ^1.1.2
  dio: ^5.4.0
  
  # Storage
  shared_preferences: ^2.2.2
  flutter_secure_storage: ^9.0.0
  
  # JSON
  json_annotation: ^4.8.1
  
  # Utils
  intl: ^0.19.0
  flutter_dotenv: ^5.1.0
  
  # Real-time Communication
  socket_io_client: ^2.0.3+1
  
  # Video Calls
  jitsi_meet_flutter_sdk: ^10.2.0
  permission_handler: ^11.0.1
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  
  # Linting
  flutter_lints: ^3.0.1
  
  # Code generation
  build_runner: ^2.4.7
  json_serializable: ^6.7.1
  
  # Testing
  integration_test:
    sdk: flutter
  mockito: ^5.4.4
  flutter_driver:
    sdk: flutter
  test: ^1.24.9

flutter:
  uses-material-design: true
  
  # assets:
  #   - assets/images/
  #   - .env
  
  # fonts:
  #   - family: Roboto
  #     fonts:
  #       - asset: fonts/Roboto-Regular.ttf
